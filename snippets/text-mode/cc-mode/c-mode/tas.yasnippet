#name : TEST_ASSERT()
#contributor : Martyn Jago
# Another way of calling TEST_ASSERT_TRUE
# Evaluates whatever code is in condition and fails if it evaluates to
#false
# --
TEST_ASSERT(${1: condition});$0